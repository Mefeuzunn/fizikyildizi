import ClientPage from './ClientPage'; export function generateStaticParams() { return [{ notfound: ['1'] }]; } export default function Page({ params }: any) { return <ClientPage params={params} />; }
